{
  "name": "Treasure Maze Dev Container",
  "dockerComposeFile": [
    "../docker-compose.yaml",
  ],
  "service": "treasure-maze",
  "workspaceFolder": "/app",
  "settings": {
    "python.defaultInterpreterPath": "/usr/local/bin/python"
  },
  "extensions": [
    "ms-python.python",
    "ms-python.vscode-pylance",
    "ms-toolsai.jupyter-renderers"
  ],
  "postCreateCommand": "python -m venv .venv && chmod +x .venv/bin/activate && . .venv/bin/activate && pip install -r requirements.txt || true"
}